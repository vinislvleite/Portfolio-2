<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title><%= editar ? 'Editar Disciplina' : 'Nova Disciplina' %> | Vinícius Leite</title>
    <link rel="stylesheet" href="/css/nova.css">
</head>
<body>
    <%- include('partials/header') %>
    <div class="container">
        <h1><%= editar ? 'Editar Disciplina:' : 'Nova Disciplina:' %></h1>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-error">
                <%= error %>
            </div>
        <% } %>
        
        <div class="form-container">
            <form action="<%= editar ? `/disciplinas/${disciplina.id}?_method=PUT` : '/disciplinas/nova' %>" method="POST">
                <div class="form-group">
                    <label for="nome">Nome da Disciplina</label>
                    <input type="text" id="nome" name="nome" class="form-control" 
                           value="<%= disciplina ? disciplina.nome : '' %>" required>
                </div>
                <div class="form-actions">
                    <button type="submit" class="btn-submit">
                        <%= editar ? 'Atualizar' : 'Adicionar' %>
                    </button>
                    <a href="/disciplinas" class="btn-cancel">Cancelar</a>
                </div>
            </form>
        </div>
        <a href="/disciplinas" class="back-link">← Voltar para a lista de disciplinas</a>
    </div>
</body>
</html>